#include<stdio.h>
int main() {
    int arr[5]={810,25,70,9,42};
    int n=sizeof(arr)/sizeof(arr[0]);
    int max=arr[0];
    for(int i=1;i<n;i++) {
        if(arr[i]>max) {
            max=arr[i];
        }
    }
    printf("Maximum number = %d\n",max);
    return 0;
}
