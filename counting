#include <stdio.h>
int main() {
    char s[]="welcome to c programming";
    int count=0,i;
    for(i=0;s[i]!='\0';i++) {
        if(s[i]==' ') {
            count++;
        }
    }
    count+=1;
    printf("%d\n",count);
    return 0;
}
